import React from "react";

type Props = {};

function Footer({}: Props) {
  return (
    <footer className="fixed bottom-0 flex flex-row items-center justify-center w-full h-12 mt-4 border-t border-1 dark:text-gray-300 dark:bg-darkTheme bg-slate-300">
      Footer
    </footer>
  );
}

export default Footer;
